for dir in $(urllist https://download.kde.org/stable/plasma/ | grep -E '^5.[0-9.]+$' | sort -rV);do
    ver=$(u2d https://download.kde.org/stable/plasma/$dir/ libkscreen-)
    if [ $ver ];then
        echo $ver
        exit 0
    fi
done
